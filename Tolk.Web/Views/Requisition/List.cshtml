@model IEnumerable<Tolk.Web.Models.RequisitionListItemModel>
@using Tolk.BusinessLogic.Utilities
@{
    ViewData["Title"] = "Rekvisitioner";
}
<div class="row">
    <div class="col-md-12">
        <h2>@ViewData["Title"]</h2>
        @if (Model.Any())
        {
            <div class="table-responsive">
                <table class="table table-striped table-hover clickable-rows" data-click-controller="Requisition" data-click-action="@Model.First().Action" data-click-parameter="id">
                    <thead>
                        <tr>
                            <th>AvropsID</th>
                            <th>Språk</th>
                            <th>Start</th>
                            <th>Slut</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                        <tr data-id="@item.RequisitionId">
                            <td>@item.OrderNumber</td>
                            <td>@item.Language</td>
                            <td>@item.Start.ToString("yyyy-MM-dd HH:mm")</td>
                            <td>@item.End.ToString("yyyy-MM-dd HH:mm")</td>
                            <td>@EnumHelper.GetDescription(item.Status)</td>
                        </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <p>
                Det fanns inga rader att visa.
            </p>
        }
    </div>
</div>
