@using Microsoft.Extensions.Options
@using Tolk.BusinessLogic.Helpers
@using Tolk.Web.Services
@inject SelectListService SelectListService
@inject IOptions<TolkOptions> options
@model RegisterViewModel
@{
    ViewData["Title"] = "Registrera användarkonto";
}
<div class="row">
    <div class="col-xs-12">
        <h1>@ViewData["Title"]</h1>
        <p>
            Fyll i dina uppgifter för att få tillgång till tjänsten. När registreringen är klar skickas en aktiveringslänk till din e‑postadress.<br />
            <b>Observera</b> att du kan bara skapa ett konto om din organisation har tillåtit att användare registrerar sig själva. Om detta inte är möjligt behöver du kontakta din centrala administratör på din organisation. Om du inte vet vem det är, kan du kontakta <a href="mailto:@options.Value.Support.FirstLineEmail">@options.Value.Support.FirstLineEmail</a> för att få hjälp.

            Använd alltid en personlig e‑postadress när du skapar ett konto.
        </p>
        <form method="post" asp-action="Register" asp-controller="Account" class="do-not-check-dirty">
            <div class="row padding-bottom-30px">
                <div class="col-xs-12">
                    <form-entry asp-for="Email" />
                    <form-entry asp-for="FirstName" />
                    <form-entry asp-for="LastName" />
                </div>
            </div>
            @if (Model != null && Model.ParentOrganisationId > 0)
            {
                <div class="row padding-bottom-30px">
                    <div class="col-xs-12">
                        <form-entry asp-for="OrganisationIdentifier" type="select" asp-items="SelectListService.SubOrganisations(Model.ParentOrganisationId)" class="allow-clear" />
                    </div>
                </div>
            }
            <div class="row">
                <div class="col-xs-12 text-align-center">
                    <button type="submit" class="btn btn-success btn-large disable-on-click">Skicka</button>
                </div>
            </div>
        </form>
        <div class="row">
            <div class="col-xs-12 text-align-center">
                Har du redan ett konto?<br />
                Gå vidare till <a asp-action="Login">Logga in <span class="glyphicon glyphicon-triangle-right"></span></a>
            </div>
        </div>
    </div>
</div>
